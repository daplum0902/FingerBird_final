<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.daplu.practice">

    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".signin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />0
            </intent-filter>
        </activity>
        <activity android:name=".Mainchat"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".MainPost"></activity>
        <activity android:name=".testJournalDetail"></activity>
        <activity android:name=".PostDetail"></activity>
        <activity android:name=".schedulelist"></activity>
        <activity android:name=".signup"></activity>



    </application>

</manifest>